{
  "name": "chatgpt-auto-allow-tool",
  "version": "0.2.3",
  "private": true,
  "description": "Chrome extension that automatically clicks Korean/English allow buttons on ChatGPT tool permission cards.",
  "scripts": {
    "check": "node --check content.js && node --check options.js && node --check popup.js && node --check scripts/package-extension.js && node scripts/validate-manifest.js && node scripts/validate-plugin-refresh.js",
    "package": "node scripts/package-extension.js"
  },
  "keywords": [
    "chrome-extension",
    "chatgpt",
    "automation"
  ],
  "license": "MIT",
  "devDependencies": {}
}
